---
description: "测试与验证门禁"
globs:
  - "tests/**"
  - "tools/**"
---

# 测试规范

## 框架与分层

| 范围 | 方式 |
|------|------|
| `tests/core/` | pytest；无硬件、无 cv2/TRT/Qt 强依赖 |
| `tests/detect/` | letterbox 等纯逻辑；TRT 测标记为需 Jetson |
| `tests/app/` | pipeline / camera_source 等；可用假帧 |
| `tests/ui/` | PySide6 + `QT_QPA_PLATFORM=offscreen` 冒烟 |
| Jetson 冒烟 | `tools/jetson_infer_smoke.py`、`tools/run_ui.sh`（手测/板上） |

## Win 本机门禁

```text
pytest tests/ -v
```

- 不得在未跑命令时声称测试通过（见 `execution-discipline.mdc`）。
- UI 测试无 DISPLAY 时用 offscreen；不要求本机有 engine 文件（可按现有用例跳过或占位）。

## Jetson 门禁（按里程碑）

| 里程碑 | 证据 |
|--------|------|
| M2 | stock engine + `jetson_infer_smoke.py` |
| M3 | `app/demo.py` / CLI 信号序列与 FSM 一致 |
| M-Bootstrap | `validation_phases.md` §1.3 清单（运行 UI 端到端） |

## 命名

- 文件：`test_<module>.py`
- 函数：`test_<behavior>`

## 不做

- 不把场内冻结集召回当作 Bootstrap commit 门禁（阶段三）。
- 不在 CI 假设存在 `*.engine` / 真机 PLC / USB 相机。
- 不测「COCO 当安全验收」。
